import { PlayingTimePipe } from './playing-time.pipe';

describe('PlayingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
